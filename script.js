document.addEventListener('DOMContentLoaded',()=>{
  let display = document.getElementById('section1');
  display.classList.add('active');
})

function Activate(sectionId){
  let sectionToShow = document.querySelectorAll();
  sectionToShow.
}